name: Echo Environment Variable

on: [push]

jobs:
  echo-env-variable:
    runs-on: ubuntu-latest
    env:
      mayur: "Hello from mayur"
    steps:
      - name: Echo the environment variable
        run: echo "The value of mayur is: ${{ env.mayur }}"
